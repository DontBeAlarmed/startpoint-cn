# 任务系统后续路线图

本文是任务模块当前阶段完成后的长期计划，描述下一步补全顺序和验收边界。它不是客户端测试记录；实际通过状态以[测试进度](../status/test-progress.md)为准。

## 当前基线

基线提交：`dev` 分支 `7c607a0`。

| 模块 | 当前服务端状态 | 客户端状态 |
|---|---|---|
| 角色觉醒 | 144 条任务均进入计算路径；55 条通用回退已按主数据审计；特殊 QuestRange、无阵亡、队长与同场条件已实现 | 核心解锁和第一页领奖已通过；144 条具体条件待逐条验收 |
| 每日任务 | 默认服务器时间下当前开放 `11/11`；常驻 all-clear 不包含活动任务 | 每日提示、发奖、跨日和重新 load 待完整验收 |
| 每周任务 | 官方当前定义 `2/2`；周一北京时间 05:00 边界和周期基线已有自动测试 | 跨周真实客户端流程待验收 |
| 称号 | 自动计算 `1129/1288`，约 87.7%；其余 159 条保留持久化 fallback | 已实现条件族的提示、领奖和持久化待验收 |
| Active Mission | 96 条定义中 40 个 pattern、86 条定义已有事实链；20011～20017 已完成 | 客户端验收由贡献者异步进行，不阻塞后续开发 |
| 活动任务 | category 3 已启用 805 条严格协力规则和 344 条关卡/物品/竞速安全规则，剩余 1363 条保持持久化 fallback | 已实现规则仍需分批客户端验收 |
| Pass | category 6/7/8 的主链、点数和等级奖励已实现 | 救援、表情、购买和主链体验待分项验收 |

## 下一阶段顺序

### 1. 补全 Active Mission 的单人可证明事实

优先处理：

1. `20017` 已按 CN 1.8.1 的主位首次弹射统计实现；
2. `20015/20016` 已从官方 CDN 建立技能动作效果索引，并匹配属性耐性下降、直接回复、比例回复和再生效果；
3. 下一批优先处理称号条件族，并继续覆盖字段缺失、异常数值、错误队伍、失败战斗和事务回滚。

缺少动作效果索引或无法确认统计语义时保持 fail closed，不按角色名称或中文文案推测。20015/20016 的
未解析角色、队伍快照边界和任务开放前是否预累计见[待审阅实现记录](mission-implementation-assumptions.md)。

### 2. 扩展简单称号

从剩余 159 条中按 pattern 分组，只选择当前存档或正式请求能够直接证明的条件。每个条件族独立完成：权威字段审计、失败测试、服务端实现、文档更新、自动测试和客户端验收。角色等级曲线、历史累计和复杂战斗状态在缺少权威数据时不实现。

已完成第一批 `pattern=48` 第二玛纳板条件族：3 条全局节点累计和 472 条指定角色全板完成。其余称号继续按同一安全下界原则逐批处理。

### 3. 分批补全活动任务与 Pass

category 3 按 QuestRange、评级、房主/成员、阶段和 client check 拆分，禁止重新启用旧宽松 map。Pass 优先处理有明确单人事实的 pattern；救援来源不明确时不冒充普通 Guest，购买流程也不与免费轨混合。

## 明确延后

- 接取救援请求和 Attention：缺少权威救援来源，保持低优先级、fail closed；
- 回归任务：资格生产者完成前不向普通玩家开放；
- Active Mission `21030`：依赖尚未实现的 `multi_special_exchange` 活动入口；
- 完整数据库导入导出：本路线图不扩展所有任务事实的迁移格式；
- 角色等级近似计算：等待 CDN 提供权威 EXP 曲线。

## 每批完成门槛

每个条件族必须独立提交，并通过：

```bash
node tools/test-workflow/run.cjs --group integration:mission
node tools/docs_check.cjs
npm run build
bash scripts/check-hygiene.sh
git diff --check
```

提交后不自动 push。客户端验收由贡献者异步进行，不作为继续开发的前置条件；规则实现、架构文档和验收结果必须分别保持中文且不互相冒充。
