# 经验池边界

## 结论

经验池的合法存档值是非负安全整数。经验池正好花完时，服务端写入并返回 `0`；“游戏内显示 `0`、存档却为负数”不是归零的正常结果，而是历史坏档、非法导入，或某个奖励/邮件写入了负数。

经验池的增长基准是数据库中的真实时间，不受全局虚拟服务器日期前后调整影响。数据库只保存真实时间；发送给客户端时由 `realToVirtual()` 转换为客户端需要的服务器时间戳，客户端仍然可以用自己的服务器时间计算经过时长。这样调整卡池或活动日期不会让经验池停止、倒退或一次性获得异常长时间的经验。

旧版本曾把 `getVirtualNow()` 写入 `exp_pooled_time`。服务端在登录或经验池结算时会识别明确属于旧虚拟时间域的锚点并迁移；未来、非法或无法安全还原的锚点会改为当前真实时间，但保留已有经验池余额，不会因为修复而清空经验池。

## 写入规则

- `/api/index.php/expod/inject_exp` 只接受正的安全整数经验消耗，拒绝负数、零、小数、非有限数和超安全整数。
- `updatePlayerSync` 与 `insertPlayerSync` 对 `expPool` 做统一非负安全整数保护。异常奖励不会静默写入负数；位于业务事务中的流程会随事务回滚。
- 数据库初始化会把历史 `exp_pool < 0` 修复为 `0`，并输出修复数量。这样客户端不会继续收到会被显示层截断的负值。

## 相关流程

```text
经验消耗请求
  -> 校验正整数
  -> 校验余额足够
  -> 事务内扣除经验池、增加角色经验、记录任务事实
  -> 返回同一事务后的 exp_pool
```

战斗、邮件、任务奖励和管理后台仍然通过玩家领域写入经验池；因此它们共享同一非负约束。若 CDN 奖励表本身包含负经验，服务端会拒绝对应经验池写入，不把 CDN 异常扩散为负值坏档。

存档导入和克隆会沿用可识别的真实时间锚点；旧虚拟时间或异常锚点按同一套迁移规则处理。后台直接修改经验池时会同时把真实时间锚点重置为当前时间，避免修改前的离线时长再次被重复结算。

## 回归测试

- `tools/expod_inject_exp_route.test.cjs`：正好归零、超额拒绝、非法消耗拒绝、失败回滚。
- `tools/exp_pool_time.test.cjs`：真实时间增长、虚拟时间切换、旧虚拟锚点迁移、异常锚点修复和客户端时间戳往返。
- `tools/test-workflow/database-lifecycle.test.cjs`：历史负经验池启动修复。
