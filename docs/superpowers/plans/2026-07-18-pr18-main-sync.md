# PR 18 Main Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Resolve PR #18 against `origin/main` while importing main's hardened mission behavior and preserving the release branch's Advent, Rush, Rogue, weapon, character-package, and asset-bridge fixes.

**Architecture:** Treat the mission subsystem from PR #17 on `origin/main` as authoritative. Keep the release branch as the integration base, merge main into it, and manually compose `singleBattleQuest.ts` so battle completion records mission dimensions without losing release-specific start rules or Rogue rewards. Validate source composition, runtime mission safety, mode regressions, release graph guards, and the repository-wide engineering gates before a non-force PR update.

**Tech Stack:** Git, TypeScript, Fastify, Node.js test runner, npm, Python `unittest`, PowerShell

## Global Constraints

- Work only in `D:\WF\startpoint-cn\.worktrees\pr18-resolution`; do not modify, stash, clean, or reset the primary worktree.
- Resolve the first seven listed conflict files from `origin/main`; manually retain both branches' behavior in `src/routes/api/singleBattleQuest.ts`.
- Preserve all release-specific asset patches and the `1.4.101 -> 1.4.102` bridge.
- Never force-push. Re-fetch and prove the PR head is still an ancestor immediately before updating it.
- Do not merge PR #18 into `main`.

## Task 1: Establish the merge contract

**Files:**

- Create: `tools/pr18_merge_contract.test.cjs`

- [ ] Add a source-level regression that requires Advent prerequisites, Rogue per-round drops, and fail-open mission-dimension recording in the single-battle route.
- [ ] Run it before merging and record the expected RED result: the release branch lacks mission-dimension recording.
- [ ] Commit the regression test and this plan as an isolated test commit.

## Task 2: Merge and resolve the eight conflicts

**Files:**

- Resolve from main: `docs/systems/mission-dimension-progress.md`
- Resolve from main: `src/lib/mission/computer-regular.ts`
- Resolve from main: `src/lib/mission/index.ts`
- Resolve from main: `src/lib/mission/patterns.ts`
- Resolve from main: `src/lib/mission/rewards.ts`
- Resolve from main: `src/routes/api/activeMission.ts`
- Resolve from main: `src/routes/api/mission.ts`
- Resolve as union: `src/routes/api/singleBattleQuest.ts`

- [ ] Merge the freshly fetched `origin/main` with a merge commit.
- [ ] Take main's complete mission implementations for the first seven conflicts.
- [ ] In `singleBattleQuest.ts`, retain release start/cost/continue and Rogue-drop behavior while adding main's safe mission-dimension writer.
- [ ] Confirm there are no conflict markers and run the merge-contract test GREEN plus `tools/mission_safety.test.cjs`.

## Task 3: Verify and update PR #18

- [ ] Run focused Advent, Rush, Rogue, shop, mission, asset-graph, and character-flow tests.
- [ ] Run `npm run verify`, launcher tests, hygiene tests, full hygiene scan, and all root/admin high-severity audit gates.
- [ ] Perform a code review of the merge diff and confirm the primary worktree remains untouched.
- [ ] Re-fetch the PR head, prove fast-forward safety, and push without force to `release/modes-20260714`.
- [ ] Confirm GitHub reports PR #18 mergeable; leave the PR open for the user to merge.
