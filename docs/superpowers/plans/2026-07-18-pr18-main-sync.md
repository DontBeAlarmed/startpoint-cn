# PR 18 Main Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Resolve PR #18 against `origin/main` while importing main's hardened mission behavior and preserving the release branch's Advent, Rush, Rogue, weapon, character-package, and asset-bridge fixes.

**Architecture:** Treat the mission subsystem from PR #17 on `origin/main` as authoritative. Keep the release branch as the integration base, merge main into it, and manually compose `singleBattleQuest.ts` so battle completion records mission dimensions without losing release-specific start rules or Rogue rewards. Validate source composition, runtime mission safety, mode regressions, release graph guards, and the repository-wide engineering gates before a non-force PR update.

**Tech Stack:** Git, TypeScript, Fastify, Node.js test runner, npm, Python `unittest`, PowerShell

## Global Constraints

- Work only in `D:\WF\startpoint-cn\.worktrees\pr18-resolution`; do not modify, stash, clean, or reset the primary worktree.
- Resolve the first seven listed conflict files from `origin/main`; manually retain both branches' behavior in `src/routes/api/singleBattleQuest.ts`.
- Preserve all release-specific asset patches and the `1.4.101 -> 1.4.102` bridge.
- Never force-push. Re-fetch and prove the PR head is still an ancestor immediately before updating it.
- Do not merge PR #18 into `main`.

## Task 1: Establish the merge contract

**Files:**

- Create: `tools/pr18_merge_contract.test.cjs`

- [x] Add a source-level regression that requires Advent prerequisites, Rogue per-round drops, and fail-open mission-dimension recording in the single-battle route.
- [x] Run it before merging and record the expected RED result: the release branch lacks mission-dimension recording.
- [x] Commit the regression test and this plan as an isolated test commit.

## Task 2: Merge and resolve the eight conflicts

**Files:**

- Resolve from main: `docs/systems/mission-dimension-progress.md`
- Resolve from main: `src/lib/mission/computer-regular.ts`
- Resolve from main: `src/lib/mission/index.ts`
- Resolve from main: `src/lib/mission/patterns.ts`
- Resolve from main: `src/lib/mission/rewards.ts`
- Resolve from main: `src/routes/api/activeMission.ts`
- Resolve from main: `src/routes/api/mission.ts`
- Resolve as union: `src/routes/api/singleBattleQuest.ts`

- [x] Merge the freshly fetched `origin/main` with a merge commit.
- [x] Take main's complete mission implementations for the first seven conflicts.
- [x] In `singleBattleQuest.ts`, retain release start/cost/continue and Rogue-drop behavior while adding main's safe mission-dimension writer.
- [x] Confirm there are no conflict markers and run the merge-contract test GREEN plus `tools/mission_safety.test.cjs`.

## Task 3: Make the character strand repair failure-atomic

**Files:**

- Modify: `mod-tools/wf_release_guard.py`
- Modify: `mod-tools/wf_release.py`
- Modify: `mod-tools/tests/test_release_guard.py`

- [x] Add RED regressions proving bridge verification, partial-copy, preparation, and atomic-publication failures leave no newly created `charbridge` archives.
- [x] Run all read-only package checks and prepare staging before creating a live bridge.
- [x] Make bridge copies atomic and track exactly which archives this invocation created.
- [x] On failure, remove only those newly created archives; retain bridges after a successful publication.
- [x] Run the focused release-guard suite GREEN and review the failure ordering and rollback boundaries.

## Task 4: Publish the committed abyss-weapon balance as a new tail edge

**Files:**

- Create: `assets/asset-patch/active/pinball-1.4.105-1.4.106-1-abyssbalance0718.zip`
- Modify: `assets/asset-patch/manifest.json`
- Create: `mod-tools/tests/test_abyss_weapon_release_patch.py`
- Create or modify a deterministic build helper only if required for repeatability.

- [x] Add a RED artifact regression requiring an enabled `1.4.105 -> 1.4.106` edge with exactly the three full orderedmap payloads.
- [x] Verify the ignored pre-suite ability-soul and weapon-ability baselines by their pinned SHA-256 hashes; copy them only to an OS temp directory.
- [x] Starting from those clean baselines, apply the current 15-weapon generator and build a deterministic three-table ZIP. Do not modify the ignored source files or the live CDN.
- [x] Assert all 15 generated values/descriptions, official non-custom soul rows, the exact official weapon-ability payload, ZIP integrity, and graph reachability to `1.4.106`.
- [x] Update the active manifest without rewriting historical archives.

## Task 5: Verify and update PR #18

- [x] Run focused Advent, Rush, Rogue, shop, mission, asset-graph, and character-flow tests.
- [x] Run `npm run verify`, launcher tests, hygiene tests, full hygiene scan, and all root/admin high-severity audit gates. The Windows launcher invocation was environment-blocked because `Get-NetTCPConnection` returned a page-file exhaustion error; Linux launcher assertions and every other gate passed.
- [x] Perform a code review of the merge diff and confirm the primary worktree remains untouched.
- [ ] Re-fetch the PR head, prove fast-forward safety, and push without force to `release/modes-20260714`.
- [ ] Confirm GitHub reports PR #18 mergeable; leave the PR open for the user to merge.
